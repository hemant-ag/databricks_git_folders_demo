# This is a Databricks asset bundle definition for databricks_git_folders_proj.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_git_folders_proj
  uuid: 79809d21-2a36-4727-91bd-04a4ea2329df

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix:  dev_${workspace.current_user.short_name}_
      source_linked_deployment: false
    workspace:
      host: https://dbc-86a222a2-9887.cloud.databricks.com/
      root_path: /Workspace/Users/hemantbansal1992@gmail.com/db_git_folders/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    presets:
      name_prefix:  prod_${workspace.current_user.short_name}_
      source_linked_deployment: false
    workspace:
      host: https://dbc-86a222a2-9887.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/hemantbansal1992@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/hemantbansal1992@gmail.com/db_git_folders/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
